{% extends 'base.html.twig' %}

{% block title %}
    new product
{% endblock %}

{% block content %}

    {{ form_start(form) }}
        <div>
            {{ form_widget(form.name) }}
        </div>
        <div>
            {{ form_widget(form.sellPrice) }}
        </div>
        <div>
            {{ form_widget(form.gamme) }}
        </div>
        <div class="container-produitFournitures">
            <div>
                {{form_widget(form.produitFournitures)}}
            </div>
            <input type="number" id placeholder="quantité">
            <button type="button" id="add-quantite">ajouter</button>
        </div>
    {{ form_end(form) }}


{% endblock %}